<nav class="navbar navbar-dark bg-dark navbar-expand-lg fixed-top fw-bold">
    <div class="container-fluid">
      <div class="collapse navbar-collapse d-flex justify-content-between">
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">
              <% if (user) { %> 
                <%= user.displayName %>
                  <% if(user.isAdmin) { %>
                    <span class="badge badge-pill badge-success"><i class="bi bi-patch-check-fill"></i>Admin</span>
                  <% } %>
                 <% } else { %>
                   iSnapBook
                <% } %>
            </a>
          </li>
        </ul>
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
        </ul>
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li class="nav-item">
            <% if(!user) { %>
            <a class="nav-link active" aria-current="page" href="/auth">Innlogging</a>
            <% } else { %>
            <a class="nav-link active" aria-current="page" href="/auth/logout">Logg ut</a>
            <% } %>
          </li>
        </ul>
      </div>
    </div>
  </nav>